const lista1 = [
    100,
    200,
    500,
    4000,
];

const mitadLista1 = lista1.length / 2;

if (lista1 es par?) {
    // necesitamos dos elementos
    // -> el promedio
    // -> mediana 
} else{
    // posicion mittad dentro de lista1
    // -> mediana
}
